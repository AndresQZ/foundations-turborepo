const internalValue = "internalEndpoint"
class Client {

    performAction(){

        console.log(`perform action`)
        console.log(`internalValue ${internalValue}`)
    }
}

export {
    Client
}